# Claude Code Settings

## Git Restrictions

Do NOT perform any git commit operations in this project. This includes `git commit`, `git commit --amend`, `git cherry-pick`, and any other commands that create commits.
